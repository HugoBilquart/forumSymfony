{% extends 'base.html.twig' %}

{% block title %}IT Solutions - Edit profile details{% endblock %}

{% block body %}
    
    {{ form_start(form) }}
	{{ form_errors(form) }}
	{{ form_rest(form) }}
	<button type="submit" class="btn btn-primary">Save</button>
	{{ form_end(form) }}
    
{% endblock %}